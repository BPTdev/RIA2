const { Given, When, Then } = require('@cucumber/cucumber');


Given('the user\'s browser is set to the "{string}" language', async function (language) {
});

When('the user accesses the web application', async function () {
});

Then('the application displays in {string}', async function (language) {

});
